# Frontend

React dashboard for the GenAIOps Support Copilot.

## Run

```bash
pnpm install
pnpm run dev
```

Set `VITE_API_BASE_URL` if the backend is not running on `http://localhost:8000`.

## Test

```bash
pnpm run test
```
