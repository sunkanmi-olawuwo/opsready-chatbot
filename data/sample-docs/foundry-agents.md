# Microsoft Foundry Agents

Microsoft Foundry Agents combine a model, instructions, and tools. A support copilot can use a Foundry Agent to reason over a question, call knowledge tools, and return a grounded response.

This project targets the newer Foundry Agent model rather than classic assistant patterns. Local code provides a mock agent so the dashboard and backend can be developed before Azure credentials are configured.
