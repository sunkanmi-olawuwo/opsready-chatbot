# Plan Divergence Tool

The plan divergence script checks whether planned features are reflected in feature coverage.

Run:

```bash
python scripts/plan_divergence.py
```

The first version is advisory and exits successfully.
