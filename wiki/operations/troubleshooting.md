# Troubleshooting

## Backend cannot find prompts

Run the backend from the `backend/` directory or set `PROMPT_DIRECTORY`.

## Frontend cannot reach backend

Set `VITE_API_BASE_URL=http://localhost:8000`.
